# LLM and API interaction
openai>=1.0.0

# Video processing (ffmpeg must be installed separately)

# Transformers and Qwen model
transformers>=4.40.0
torch>=2.0.0
accelerate>=0.25.0
qwen-omni-utils>=0.0.4

# NLP
rank-bm25>=0.2.0

# Vector search and embeddings
faiss-gpu>=1.12.0
# Note: If GPU version has issues, can fallback to: faiss-cpu>=1.7.0

# Scientific computing
numpy>=1.24.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0

# Configuration and utilities
python-dotenv>=1.0.0
PyYAML>=6.0
tqdm>=4.65.0

# Logging and debugging
coloredlogs>=15.0.0
